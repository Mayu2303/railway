class SearchTrain {
  String from;
  String to;

  SearchTrain(this.from, this.to);

  @override
  String toString() {
    // TODO: implement toString
    return "$from -> $to";
  }

  ///toFind(this.from,this.to);
}
